export type Department = {
  id: number;
  name: string;
  engagement_code: string;
  correspondence_code: string;
};

export type Signatory = {
  id: number;
  full_name: string;
  initials: string;
};